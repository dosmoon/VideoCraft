# VideoCraft runtime dependencies — all versions pinned for reproducible builds.
# To upgrade: bump the version, `pip install -r requirements.txt --upgrade`,
# test, commit. Lockfile / uv migration tracked as a separate plan.

# Core
yt-dlp==2026.3.17
requests==2.32.5
srt==3.5.3

# Translation
deepl==1.24.0

# AI providers (LLM via google-genai; legacy google-generativeai removed
# 2026-04-30 — see commit a082151)
google-genai==1.55.0
fish-audio-sdk==1.3.0
openai==2.30.0

# Local ASR — faster-whisper bundles CTranslate2 + tokenizers; CUDA is
# auto-used when ctranslate2 detects an available device.
faster-whisper==1.2.1

# Local ASR (European-language tier) — Parakeet TDT 0.6B v3 via NVIDIA NeMo.
# nemo_toolkit pulls torch / pyarrow / pandas / datasets transitively. The
# version pins below pre-empt a Windows-specific access violation in
# pyarrow 24.x and the pandas 3.0 / datasets 4.x ABI mismatch they ship
# alongside (see commit log for the original crash trace).
nemo_toolkit[asr]==2.7.3
pyarrow==19.0.1
pandas==2.3.3
datasets==3.6.0

# Local ASR (Asian-language tier) — SenseVoiceSmall via Alibaba FunASR.
# torchaudio is a hidden dep of funasr.utils.load_utils — pip install funasr
# alone does NOT pull it on Windows, so we pin it explicitly.
funasr==1.3.1
torchaudio==2.11.0

# Media
Pillow==12.0.0

# Windows-only
pywin32==311; sys_platform == 'win32'
