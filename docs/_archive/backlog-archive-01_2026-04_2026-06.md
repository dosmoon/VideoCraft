# BACKLOG 归档卷 01（2026-04 ~ 2026-06-10）

> 2026-06-10 从 [`docs/BACKLOG.md`](../BACKLOG.md) 拆出：已完成、已失效（随 Tk 退役）、已被现实解决、以及历史裁决全文。
> 现行待办只看 `docs/BACKLOG.md`；本文件只读不更新。

---

## ✅ 已完成

| 优先级 | 功能 | 说明 |
|--------|------|------|
| 🟢 P3 | requirements.txt → uv + lockfile | **2026-06-04 完成（ADR-0009）**：迁到正统 uv 项目 —— `pyproject.toml` 做全部 pin 的单源（base = `[project.dependencies]`，embedded-ai/gpu = extras，dev/build = dependency-groups）+ `uv.lock` 锁死整个传递闭包（之前传递依赖浮动）。`build_sidecar.ps1` 走 `uv sync --frozen`；dev myenv 经 `uv sync --extra embedded-ai` 净化重建（7GB→391MB）；运行时档 pin 由 `tests/core/test_dependency_pins.py` 防漂移。`requirements*.txt` 已删 |
| 🟢 P3 | TTS Voice ID 收藏 | 2026-05-11 完成 — 整套 TTS 抽象重构 (P1~P6)：`core/ai/tts_voice.py` (TTSVoice 数据模型 + 磁盘缓存)、VoicePickerDialog 跨 provider 选择 + ffplay 试听 + 手动输入 ID、AI 控制台独立 TTS tab、text2video 单声 + 多角色每角色独立选引擎、`ai.tts(provider=...)` 必填。Fish Audio "我的收藏" workaround：v1 API 没 /me/marks 端点，做了 [导入收藏 voice IDs...] 入口让用户粘贴 ID 列表逐个 GET /model/{id} 拉 metadata。详见 commits 67845f8..d6b6336（注：Tk 时代 UI 已退役，TTSVoice 数据层与原则仍在） |

**内嵌 AI（Embedded AI Tier）已完成子任务**（详细设计原稿：[tech-selection-embedded-ai.md](tech-selection-embedded-ai.md)）：

| 优先级 | 子任务 | 说明 |
|--------|------|------|
| 🔴 P1 | faster-whisper ASR provider（CTranslate2） | 2026-05-10 加（commit 99ef873）。`core/ai/providers/faster_whisper.py`。CT2 真批处理 GPU kernel，4060 fp16 实测 **RTF 35.8x**（whisper-small）。built-in silero VAD。catalog 加 small (480MB) + large-v3-turbo (1.6GB) 两档。**取代 sherpa-onnx Whisper**（其后端串行 decode，RTF 上限 ~10x） |
| 🔴 P1 | edge-tts 在线免费 TTS | 2026-05-11 上线，取代 sherpa Kokoro。Microsoft Edge Read-Aloud 端点（rany2/edge-tts，MIT），新闻级中文/英文音质，免 key、零本地模型。详见 [../research-notes/sherpa-detour.md](../research-notes/sherpa-detour.md) |
| 🔴 P1 | llama-cpp-python Qwen3 翻译 provider | 2026-05-10 落地 — `core/ai/providers/llama_cpp.py` (call/call_json/list_models)；新 ptype `llama_cpp` 接进 router；模型放 `<models>/llama/*.gguf` 用户自取；GPU auto (n_gpu_layers=-2 sentinel)；4060 实测 **70 tok/s** (Qwen3-1.7B Q4_K_M)。pip install 必须用 abetlen 预编译索引（见 `pyproject.toml` [tool.uv.sources] + `core/embedded_ai_install.py`） |
| 🟡 P2 | GPU 加速升级 | 2026-05-10 落地 commit `3f1f4a8`。`core/gpu.py` 自动 PATH 设置 + cuda_available() 元数据探测（2026-05-11 改为 nvidia-smi + nvidia/* 目录双重检测）。**绝对禁止装 torch 到此 venv**（会触发 cudnn DLL 冲突，详见 ADR-0009 的 gotchas） |
| 🔴 P1 | 模型分发系统 | 2026-05-10 落地 — `core/models/{catalog,downloader,registry,manager,hf_api}.py`（UI 现为 Electron ModelManager，菜单 AI → 本地模型管理）。Range 续传 + 多源 fallback (hf-mirror/HF) + sha256 (HF lfs.oid 自动获取) + 磁盘预检 + 队列 UI。catalog 4 项 (faster-whisper small + large-v3-turbo + qwen3 1.7B + qwen3 8B)，sherpa 系全删。性能优化：scan 跳过 sha256 重哈希（每 5s 冻 UI 10s 的 bug，b39ae4d 修） |

**历史完成项的设计文档指针**（原「已完成 ✅」节）：
- AI 架构 / 路由 / 取消 / 错误契约 → [../design/04-ai-router.md](../design/04-ai-router.md)
- 文件命名 / unit 目录 → [../design/09-file-naming-convention.md](../design/09-file-naming-convention.md)
- 媒体格式模块 → [10-media-format-modules.md](10-media-format-modules.md)（Tk 时代，已归档）
- 项目工作台 / Hub → [../design/02-project-model.md](../design/02-project-model.md) + [03-ui-hub.md](03-ui-hub.md)（Tk Hub，已归档）
- 产品战略 → [../design/08-product-strategy.md](../design/08-product-strategy.md)

---

## ⚰️ 随 Tk 退役失效（所指代码已删，条目作废）

> Tk 整体退役于 2026-06-03（ADR-0008 P2）。以下条目针对的 UI / 模块不复存在；若同类痛点在 Electron 壳再现，以新语境另立新条。

| 原优先级 | 功能 | 原说明（节录）+ 失效原因 |
|--------|------|------|
| 🔴 P1 | 项目工作台 UX + 健壮性深度优化 | 针对 Tk 项目工作台 7-step 表单（卡片信息层级 / cross-step affordance / resolver 收口 / validator i18n）。**manifest 管线已退役（P6 清理 2026-05-12）+ Tk 工作台已删**；Electron 形态是 hub + 各创作工作台，不存在 7-step 表单 |
| 🟡 P2 | extract-clip / auto-split 业务逻辑下沉到 core | `src/tools/video/video_tools.py` 的 keyframe utilities 与 `core/video_split` 重复。**video_tools.py 已随 Tk 删除**，重复不复存在 |
| 🟢 P3 | 各工具窗口风格统一 | Tk 各工具窗口风格不一。**Tk 工具窗口已全删**；Electron renderer 天然单一风格体系 |
| 🟢 P3 | 输出路径可自定义 | 针对 Tk 工具（yt-dlp / speech2text / video_tools / subtitle_tool / translate_srt）的输出路径。**这些工具 UI 已删**；新架构产物路径由项目目录结构约定（ADR-0005） |
| 🟢 P3 | 操作参数持久化 | Tk subtitle_tool 的 preset 系统（user_data/presets/subtitle_burn.json）。**工具已删**；preset 概念已在新壳另行落地（clip / news_desk 创作 preset） |
| —（需求池）| Tab 工具面板可滚动布局 | 给 Tk ToolFrame 加 Canvas+Scrollbar 滚动容器。**ToolFrame 已删** |
| 🟢 P3 | per-(task, provider) prompt 变体 | 扩展中央 prompts 文件命名为 `<task>.<provider>.md`。**Prompt hub 已退役**（prompt 回插件内方向，见 design/04-ai-router.md §原则 5）；若插件内 prompt 方案落地后仍有 per-provider 差异痛点，在插件作用域内重新设计 |

---

## 🔭 已被现实解决的观察项

### 视频引擎选型监控：WebView+ffmpeg 局限性观察（原探索方向 P2）

原文记录的是「WebView2 Canvas 预览 + ffmpeg drawtext/libass 渲染」双端路线的已知局限（字体预览/渲染始终有 delta、drawtext 无原生换行/圆角/动效、filter_complex 拼接易踩坑、复杂叠加不可维护），并设了触发标准：「同一类一致性 bug 反复修第 3 次，或某类视觉效果无法在 ffmpeg 表达」即启动自建渲染引擎，候选路线含 Skia/Cairo、Bun+canvas、Rust+wgpu。

**结局（2026-05 末）**：未等触发标准凑满，Electron 迁移决策直接转向**自建 GPU 合成器**（WebGPU/Canvas 合成 + WebCodecs 编解码 + libass-wasm，preview ≡ render 单管线），经 substrate spike（Phase 0-2）验证后落地，现为生产渲染引擎。见 [../design/composition-otio-foundation.md](../design/composition-otio-foundation.md) §4 + [substrate-spike-findings.md](substrate-spike-findings.md) + ADR-0006/0008。本条目使命完成。

---

## 📺 旧「节目生成（Program Generation）」规划（core/program 方案，已被创作插件架构取代）

> 原规划：源视频 → 「节目稿子」（Markdown + YAML frontmatter 结构化文本，新增 `core/program/` 模块）→ 稿子驱动视频合成。
> **取代关系**：「稿子层 + 视频层」两层方案已被「创作插件 + OTIO composition」架构吸收——AI 产出的是创作实例内的结构化数据与 timeline（AI 写意图不写 timeline，ADR-0006），不再有独立的中间稿子格式与 `core/program/`（相关代码已在 clip 重构期删除，见 [program-script-clip.md](program-script-clip.md)）。

原 5 种形态规划及去向：

| 原优先级 | 形态 | 去向 |
|--------|------|------|
| 🔴 P1 | 切片稿（Clip Script） | ✅ **已落地为 clip 创作插件**（AI 切片工作台：样式 / 候选 / 导出），对标 OpusClip 的能力按创作插件形态实现 |
| 🟡 P2 | 摘要稿（Summary Script） | 未做；作为未来创作插件候选（见 docs/BACKLOG.md「节目形态扩展」） |
| 🟡 P2 | 解读稿（Commentary Script） | 未做；同上 |
| 🟢 P3 | 对话稿（Dialogue Script） | 未做；同上（多角色 TTS 依赖 TTS 抽象层，已具备） |
| 🟢 P3 | 剧场稿（Theater Script） | 未做；同上 |

原「共通设计决策」（稿子格式 / 是否内嵌视频指令 / 一把生成 vs 协作迭代 / 共享 schema）的回答已由现行架构给出：结构化数据 + OTIO timeline、组件化、快照原则、per-插件独立（[[feedback_no_universal_standard]]）。news_desk（新闻编导）作为形态 #3 已另线落地。

---

## ⏸ 暂缓 / 不做 —— 历史裁决全文

> 精简版在 [`docs/BACKLOG.md`](../BACKLOG.md)；此处保留完整理由与日期，防止重复议事。

| 功能 | 原因 |
|------|------|
| 云化 / SaaS | 视频处理算力消耗大，并发/等待/成本三重问题 |
| Docker 分发 | 目标用户不具备 Docker 使用背景 |
| 跨平台（短期） | 先把 Windows 版做稳，Mac 需求出现时再考虑本地 Web 方案 |
| 批量处理 | 工作量大，先做单文件质量，后续再扩展 |
| PPT / Slidev 转视频 | 受众与现有"视频创作者"主线错位（教育 / 培训 vs 自媒体），与 text2video 重叠 65%+；AI-PPT-to-video 工作流尚未被市场验证。如未来要做应另起新软件，不容纳进本 repo。2026-04-30 删除 ppt2video / slidev_pipeline / pptx_pipeline / node_env 全部相关代码。（注意与 [media-segment-composer 草稿](../draft/media-segment-composer.md) 区分：后者是「PPT 形态的媒体分段编排」创作设想，不是 PPT 文件转视频管线） |
| 字幕处理综合工作台 | 草案写完后世界变了：`subtitle.pack` 一键管线已覆盖 95% 流水线场景（一次 AI 调用产 titles+segments+refined），项目工作台 step5_pack 也包揽了「项目流程」中的字幕后处理。剩余 5% 的"手工 review/编辑"场景，用户用文本编辑器改 .txt 即可，不值得专做工作台。2026-04-30 决定取消（语境为 Tk 菜单，菜单已随 Tk 删除；痛点若再现按 Electron 工作台语境重评） |
| SRT 时间轴整体偏移 | 服务的痛点是"用户拿外部字幕烧录对不齐"，但实际场景里 VideoCraft 自家的 ASR + 翻译 + pack 流程产出的字幕都是对齐的，外部字幕导入烧录是边缘场景。等真有用户反馈再做。2026-04-30 取消 |
| SRT 格式转换（SRT↔VTT/ASS） | 服务的痛点是"用户做完视频要传 B 站 / 嵌网页"，但目标用户群（自媒体视频创作者）多数全程 SRT 走完，且烧录是终态产物（视频里就有字幕），不需要外发字幕文件。等真有用户反馈再做。2026-04-30 取消 |
| AI 响应缓存 (X4) | 当前所有 AI 入口（translate / ASR / pack / TTS）都是单次性操作 —— 跑完产物落盘，没有「同一份输入反复跑同一份 prompt」的 loop 场景。缓存现在做反而会让用户改 prompt 后困惑「为什么还是老结果」。`cache_hint=` API 位先留着不动。等将来开发 agent 类功能（自循环调用、多轮工具调用 / 反思 loop）时再补。2026-05-01 决定暂不做 |
| 拆出独立仓库 easy-ytdlp（MIT） | 原 P1。原理由"YouTube 下载是 ToS 灰区,不适合跟 VideoCraft 商业层绑定"。2026-05-11 重新评估:VideoCraft 是 MIT 全开源单机工具,**本地化处理本身就是版权护城河** —— 用户在自己机器上跑 yt-dlp,VideoCraft 作为工具提供者(类比螺丝刀厂商)由使用者自行承担版权判断。叠加「YouTube 链接一站式输入 + 首次免责弹窗」的 disclaimer 闭环,法律风险已降到可接受。**保留 yt-dlp 作为 VideoCraft 内部组件**反而能撑起"粘链接→出切片"的核心一站式体验,正是新用户期望的入口形态。等真出问题再重启拆分讨论 |
